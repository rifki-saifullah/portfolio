{
	# Caddy manages HTTPS automatically for the configured hosts.
}

{$DOMAIN} {
	reverse_proxy client_prod:3000
}

www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}

api.{$DOMAIN} {
	reverse_proxy server_prod:3000
}